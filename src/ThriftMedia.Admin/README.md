# ThriftMedia Admin

Store administration portal built with Angular.

## Development server

Run `npm start` for a dev server. Navigate to `http://localhost:5001/`. The application will automatically reload if you change any of the source files.

## Build

Run `npm run build` to build the project. The build artifacts will be stored in the `dist/` directory.
